<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Estadísticas</title>
    <link rel="stylesheet" href="https://www.w3schools.com/w3css/4/w3.css">

    <style>
        .container {
            display: flex;
            gap: 20px;
        }
        .side {
            flex: 1;
        }
        .middle {
            flex: 0.5;
        }
        .form-group {
            margin-bottom: 15px;
        }
    </style>



</head>
<body>
<div class="w3-container w3-light-grey w3-padding-16">
    <h2 class="w3-center">Estadísticas</h2>
</div>
<div class="w3-container container w3-padding-16">
    <div class="side w3-light-grey w3-padding">
        <h3>Filtrar datos</h3>
        <form id="filterForm" class="w3-container">
            <div class="form-group">
                <label for="pais">Buscar por País:</label>
                <input id="pais" class="w3-input w3-border" type="text" placeholder="Ej. Argentina">
            </div>
            <div class="form-group">
                <label for="sexo">Buscar por Sexo:</label>
                <select id="sexo" class="w3-select w3-border">
                    <option value="" disabled selected>Seleccione</option>
                    <option value="M">Masculino</option>
                    <option value="F">Femenino</option>
                    <option value="Otro">Otro</option>
                </select>
            </div>
            <div class="form-group">
                <label for="edad">Buscar por Edad:</label>
                <input id="edad" class="w3-input w3-border" type="number" placeholder="Ej. 25">
            </div>
            <div class="form-group">
                <label for="nombre">Buscar por Nombre:</label>
                <input id="nombre" class="w3-input w3-border" type="text" placeholder="Ej. Juan">
            </div>
            <button type="button" class="w3-button w3-blue w3-margin-top">Buscar</button>
        </form>
    </div>
{{#data}}
    <div class="middle w3-white w3-padding w3-border">
        <h3 class="w3-center">Gráficos</h3>
        <div>
            <h4>Usuarios Creados:</h4>
            <p>{{data.usuariosCreados}}</p>
        </div>
        <div>
            <h4>Partidas Creadas:</h4>
            <p>{{data.partidasCreadas}}</p>
        </div>
        <div>
            <h4>Preguntas Creadas:</h4>
            <p>{{preguntasCreadas}}</p>
        </div>
    </div>

    <div class="side w3-light-grey w3-padding">
        <h3>Resultados</h3>
        <div id="resultados">
            <div id="myChart" style="max-width:700px; height:400px"></div>
        </div>
    </div>
</div>
{{/data}}
</body>

<div id="grafico" data-datos="{{chartData}}"></div>

<canvas id="miGrafico" width="400" height="200"></canvas>
<script src="https://www.gstatic.com/charts/loader.js"></script>
<script src="/Monquiz/app/public/js/grafico.js"></script>
</html>
